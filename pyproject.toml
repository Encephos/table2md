[build-system]
requires = ["setuptools>=61.0", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "html-table-rescuer"
version = "0.2.0"
description = "Extracts HTML tables and converts them to Markdown, preserving structure via row/colspan handling."
readme = "README.md"
authors = [{ name = "Encephos", email = "magiltraun@gmail.com" }]
license = { text = "MIT" }
classifiers = [
    "Programming Language :: Python :: 3",
    "Programming Language :: Python :: 3.9",
    "Programming Language :: Python :: 3.10",
    "Programming Language :: Python :: 3.11",
    "Programming Language :: Python :: 3.12",
    "Programming Language :: Python :: 3.13",
    "License :: OSI Approved :: MIT License",
    "Operating System :: OS Independent",
]
requires-python = ">=3.9"
dependencies = [
    "beautifulsoup4>=4.12.0",
    "lxml>=4.9.0",
    "typer>=0.12.0",
    "requests>=2.31.0",
]

[project.scripts]
table2md = "table2md.cli:app"

# Optionale Abhängigkeiten (Extras)
[project.optional-dependencies]
dev = [
    "pytest>=8.4.2",
    "ruff>=0.8.0",
]
langchain = [
    "langchain-core",
    "langchain-community",
]

[project.urls]
Homepage = "https://github.com/Encephos/table2md"
Repository = "https://github.com/Encephos/table2md"

# Teilt setuptools mit, wo der Code liegt
[tool.setuptools.packages.find]
where = ["src"]

[tool.setuptools.package-data]
table2md = ["py.typed"]

[tool.ruff]
line-length = 100
target-version = "py39"

[tool.ruff.lint]
# Bewusst minimales Set: Fehler + Imports, kein Umstylen von Bestandscode
select = ["E4", "E7", "E9", "F", "I"]

[tool.pytest.ini_options]
testpaths = ["tests"]
